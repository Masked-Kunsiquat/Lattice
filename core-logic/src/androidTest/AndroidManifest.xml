<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Required for WorkManager foreground service on API 31+ -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <application>
        <!--
            Remove WorkManager's auto-initialization ContentProvider so that
            WorkManagerTestInitHelper.initializeTestWorkManager() can configure
            WorkManager with the test worker factory before any test runs.

            Without this, the WorkManagerInitializer ContentProvider fires at
            application startup and sets the default WorkManager instance, causing
            an IllegalStateException when the test's setUp() tries to re-initialize
            it with a custom Configuration.
        -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.impl.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

        <!--
            Declare the foreground service type so WorkManager can call
            setForeground(ForegroundInfo(..., FOREGROUND_SERVICE_TYPE_DATA_SYNC))
            without crashing on API 29+. Mirrors the declaration in :app's manifest.
        -->
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="dataSync"
            tools:node="merge" />
    </application>
</manifest>
